version: "3.9"

services:
  python-dev:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: python-dev
    tty: true
    stdin_open: true
    volumes:
      - ./workdir:/workspace
    working_dir: /workspace